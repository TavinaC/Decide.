name: decide2
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  confetti: ^0.8.0
  flutter:
    sdk: flutter
  flutter_fortune_wheel: ^1.3.2
  # flutter_soloud: ^3.4.6
  flutter_soloud: ^3.4.6
  google_fonts: ^6.3.2
  intl: ^0.20.2
  rxdart: ^0.28.0
  shake_gesture: ^2.0.0
  # shake_gesture_test_helper: ^2.0.0
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/sounds/
